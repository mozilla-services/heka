.. _multi_graph_views:

Multi-graph (Output/Views)
==========================
The multi-graph view uses a single plugin to partition the incoming data and
and output each partition as its own view.

.. literalinclude:: ../../../sandbox/lua/testsupport/telemetry_channel.lua
    :language: lua 
